<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.route.chatappc34">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ui.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChatAppC34">
        <activity android:name=".ui.roomDetailsActivity.RoomDetailsActivity"
            android:theme="@style/Theme.ChatAppC34.noActionBar"
            />
        <activity android:name=".ui.addRoom.AddRoomActivity"
            android:theme="@style/Theme.ChatAppC34.noActionBar"
            />
        <activity android:name=".ui.home.HomeActivity"
            android:theme="@style/Theme.ChatAppC34.noActionBar"
            />
        <activity android:name=".ui.register.RegisterActivity"
            android:theme="@style/Theme.ChatAppC34.noActionBar"
            />
        <activity android:name=".ui.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>